# Synapse Environment Configuration for Server 107.189.19.66
# Customized for your server deployment

# Domain Configuration - Using IP address for initial setup
DOMAIN=107.189.19.66
PUBLIC_URL=http://107.189.19.66:8008

# Database Configuration
POSTGRES_USER=synapse_user
POSTGRES_PASSWORD=SynapseSecure2024!
POSTGRES_DB=synapse

# Redis Configuration
REDIS_PASSWORD=RedisSecure2024!

# Volume Paths (absolute paths for server deployment)
VOLUME_PATH=/opt/synapse

# Synapse Configuration
SYNAPSE_CONFIG_DIR=/data
SYNAPSE_CONFIG_PATH=/data/homeserver.yaml
SYNAPSE_REPORT_STATS=no

# Security
SYNAPSE_REGISTRATION_SHARED_SECRET=YourRegistrationSecret2024!

# File permissions (recommended for Linux servers)
UID=991
GID=991

# Docker Compose File Selection
COMPOSE_FILE=docker-compose.yml

# Server specific settings
SYNAPSE_HTTP_PORT=8008
SYNAPSE_HTTPS_PORT=8448
